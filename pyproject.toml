[project]
name = "dw_audit_bot"
version = "0.0.1"
dependencies = [
    "hikari",
    "hikari-crescent",
    "python-dotenv",
    "pandas",
    "uvloop",
    "google-re2>=1.1.20240702",
    "aiosqlite>=0.21.0",
    "sqlalchemy>=2.0.42",
    "polars-lts-cpu>=1.32.2",
]

[project.scripts]
dw_audit_bot = "bot:main"

[dependency-groups]
dev = [
    "ruff>=0.11.13",
]

[tool.ruff]
line-length = 120
